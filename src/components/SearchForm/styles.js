import styled from "styled-components";

export const StyledForm = styled.form`
    display: inline;
    position: relative;
    svg {
        position: absolute;
        top: 50%;
        right: 1rem;
        transform: translateY(-50%);
    }
`;
