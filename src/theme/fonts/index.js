import colors from "../colors";
import { appFontFaces } from "./faces";
import { fontVariants } from "./variants";

const font = {
    defaults: {
        color: colors.brandBlack,
        fontFamily: appFontFaces.bodyFont,
    },
    fontVariants,
};

export default font;
